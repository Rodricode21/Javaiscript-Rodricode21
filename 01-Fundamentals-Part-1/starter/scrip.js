// let js = "amazing";
//       if (js=== "amazing")alert("javascript is FUN!");
//       40 +60 +30 + 21; 
//       console.log (40 +60 +30 + 21); 
/*
const bill=275;
// const tip = bill <= 300 && bill >= 50 ? bill* 0.15 : bill * 0.20; 
const tip = bill >= 50 && bill <= 300 ? bill * 0.15 : bill * 0.20;
let total= bill+ tip ;
console.log(`the bill was ${bill}, the tip was ${tip}, and the total value is ${total}`);


function numberSolution(number) {
  // Entender cómo funciona el modulus operator

  // números que sí pasaron el check
  const toSumNumbers = [];

  // iterate para llegar al 8
  for(let i = 1; i <= number; i++) {
    if (i % 3 === 0 || i % 5 === 0) {
      toSumNumbers.push(i);
    }
  }

  console.log('toSumbNumbers', toSumNumbers);

  // Hacer checks *ifs/else* para ver si el número que se pase es modular o no. 
};

numberSolution(8);*/


// haciendo ejercios de otros cursos o paginas random

